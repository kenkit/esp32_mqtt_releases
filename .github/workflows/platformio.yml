name: PlatformIO CI

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
          key: ${{ runner.os }}-pio
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install PlatformIO Core
        run: pip install --upgrade platformio
      - name: Install lzss
        run: pip install lzss
      - name: Install crccheck
        run: pip install --upgrade crccheck
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Clone private repo
        run: git clone git@github.com:kenkit/esp32_mqtt_server.git

      - name: Build PlatformIO Project
        run: pio run -d esp32_mqtt_server/ -e esp32-devkitc-v1

      - name: Compress firmware
        run: python3 lzss.py --encode esp32_mqtt_server/.pio/build/esp32-devkitc-v1/firmware.bin firmware.lzss

      - name: Convert to OTA
        run: python3 bin2ota.py ESP32 firmware.lzss firmware.ota

      - name: Build Changelog
        id: github_release
        uses: mikepenz/release-changelog-builder-action@v5
        env:
          GITHUB_TOKEN: ${{ secrets.WORKFLOW_ACTION }}

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.WORKFLOW_ACTION }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: ${{steps.github_release.outputs.changelog}}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.WORKFLOW_ACTION }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: firmware.ota
          asset_name: firmware-${{ github.ref_name }}.ota
          asset_content_type: application/octet-stream
